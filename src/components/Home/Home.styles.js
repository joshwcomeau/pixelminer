import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
  home: {
    position: 'relative',
    minHeight: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  bankInfoWrapper: {
    position: 'absolute',
    top: '1rem',
    left: 0,
    right: 0,
    textAlign: 'center',
  }
});

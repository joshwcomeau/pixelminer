import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
  home: {
    position: 'relative',
    minHeight: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  bankInfoWrapper: {
    position: 'absolute',
    top: '1rem',
    right: '1rem',
    textAlign: 'right',
  }
});

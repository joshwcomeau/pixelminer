import { StyleSheet } from 'aphrodite';
import { white } from '../../constants/colors';

export default StyleSheet.create({
  clickablePixel: {
    width: '100px',
    height: '100px',
    borderRadius: '10px',
    background: white,
    outline: 'none',
  },
});
